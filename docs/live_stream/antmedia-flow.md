# Stream created:
Teacher/LiveProgramController → createStream() (Ant Media)

# Stream reused:
findExistingStream() used in Teacher, Student, LiveController

# Join info returned:
getStream() methods (Student & LiveController) return streamId